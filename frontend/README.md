# React dashboard source

The zero-dependency dashboard served by FastAPI is the offline demo surface. This folder also reserves a Vite/React build target for a production component migration; install its `package.json` dependencies only when network access is available. It has no cloud runtime dependency.
